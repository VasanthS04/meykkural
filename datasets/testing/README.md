# Testing Dataset

Place short WAV files used for local smoke tests in this directory.
Audio is not required to start the application; the checked-in Wav2Vec2
checkpoint is used by the backend model tests.